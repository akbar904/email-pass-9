name: com.example.simpleapp
description: A simple Flutter application demonstrating the Cubit pattern for state management with a login and home screen.
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.1.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true